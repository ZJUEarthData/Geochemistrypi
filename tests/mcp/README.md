# MCP tests

This suite will contain protocol, stdio-purity, tool-contract, cancellation,
idempotency, and restart tests after the MCP package is introduced.

PR0 creates the boundary only; it does not add an MCP dependency to the Python
3.9 engine environment.
